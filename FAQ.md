# Momo Song v4 FAQ

## ローカルとRemoteの違いは？

Local acestep.cppは、作詞にローカルGemma、作曲にローカルACE-Stepを使用し、画像生成は
行いません。Remoteは設定したOpenAI互換LLM、ACE-Step REST API、画像生成APIを使用します。

## どのモデルを選べばよいですか？

まずTURBO-Q4を使用してください。12GB VRAMで余裕があればTURBO-Q8、16GB VRAMでは
XL TURBOを選択できます。必要量は環境や曲長でも変わるため、表記は目安です。

## STRICTとは？

生成工程で不要なモデルを解放してVRAMを抑えるモードです。モデル常駐より切替時間が
増えます。TURBO-Q4との組み合わせが省VRAM向けです。

## モデル変更はいつ反映されますか？

GUIで選択後、次の生成直前にACEサーバーを必要に応じて再起動して反映します。同じ設定で
続ける場合は再起動しません。

## ループ生成を止めるには？

「ループ生成 ON」を再度押すか、再生中の画像エリアをクリックします。画像クリックでは
音声も停止し、手動モードへ即座に戻ります。

## ローカルで画像が生成されないのは異常ですか？

仕様です。選択した背景が維持されます。画像生成はRemoteバックエンドだけで行います。

## 長い歌詞が見切れます

再生位置に合わせて歌詞部分だけが自動スクロールします。末尾が表示領域へ収まる位置で
停止し、タイトルはスクロールしません。スクロールバーは表示されません。

## 生成履歴は永続保存されますか？

いいえ。履歴はブラウザの接続セッションごとに分離された一時データです。必要な曲は
個別保存または一括ダウンロードしてください。

## 2曲目に前の歌詞が表示されます

現行版では生成開始時と再生曲切替時に表示データを分離しています。再現する場合は、操作、
ブラウザ、サーバーログと直前の設定を添えてIssueで報告してください。

## 対応Pythonは？

初期インストーラはPython 3.10〜3.12を対象とします。対応する公式
`llama-cpp-python`ホイールがない環境では、ソースビルドへ切り替えず停止します。
