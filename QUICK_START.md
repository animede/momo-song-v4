# Momo Song v4 クイックスタート

## インストール

Python 3.10〜3.12を使用します。依存物は各上流配布元から直接取得されます。

```bash
# Linux / CUDA 12.5
./install.sh --backend cuda --cuda cu125

# 起動
.venv/bin/python music_server.py
```

Windows PowerShellでは次のように実行します。

```powershell
.\install.ps1 -Backend cuda -Cuda cu125
.\.venv\Scripts\python.exe music_server.py
```

ブラウザで <http://localhost:64653> を開きます。モデルウェイトは同梱されないため、
README記載のパスへ別途配置してください。

## 基本操作

1. 曲のイメージを入力します。空欄なら内容も自動で決まります。
2. 必要なら「生成パラメータ」を開き、ジャンル、モデル、長さなどを設定します。
3. 「音楽生成」を押します。作詞完了後、歌詞と解説が表示され、作曲を続行します。
4. 完成した曲は自動再生され、「生成した曲」の一覧へ追加されます。

ローカルバックエンドでは画像生成を行わず、選択した背景を維持します。Remoteでは
設定済みの画像生成APIを利用します。

## 主なボタン

- **音楽生成**: 1曲生成します。
- **初期化**: 表示と入力状態を初期状態へ戻します。
- **ループ生成**: 曲の再生終了後に次の曲を続けて生成します。画像エリアをクリックすると
  再生を停止し、即座に手動モードへ戻ります。
- **保存**: 現在の曲を保存します。
- **一括ダウンロード**: セッション内の生成曲を、MP3と同名の歌詞TXTを含むZIPで保存します。

生成履歴はブラウザの接続セッション単位で保持され、最大20行をスクロール表示します。

## ローカル向け推奨設定

- 既定モデル: `TURBO-Q4（標準／STRICT時6GB～）`
- 既定VRAMモード: `モデル常駐（高速）`
- VRAMを抑える場合: `STRICT（省VRAM）`
- 12GB VRAMで品質を上げる場合: `TURBO-Q8（高品質／12GB～）`
- 16GB VRAM向け: `XL TURBO（超高品質／16GB～）`

モデルまたはVRAMモードの変更は、次回生成時にローカルACEサーバーへ反映されます。
