# Momo Song v4 ユーザーガイド

Momo Song v4は、歌詞生成、ACE-Step 1.5による作曲、背景と音響連動演出をまとめた
Webアプリケーションです。画面例は[README](README.md#gui-example)を参照してください。

## 画面構成

- **ヘッダー**: 背景と演出を選択します。背景には手動、自動、ランダムの選択肢があります。
- **画像・歌詞エリア**: 曲名を固定表示し、長い歌詞だけを曲の進行に合わせてスクロールします。
- **音楽の解説・プレーヤー**: 解説と音声プレーヤーを横並びで表示します。
- **入力・操作**: 曲の指示、音楽生成、初期化、ループ生成、保存を操作します。
- **生成パラメータ**: 詳細設定を折りたたみ表示します。
- **生成した曲**: 現在の接続セッションで生成した最大20行の履歴です。

## 背景と演出

背景の「自動」は曲の内容に合う画像を選び、「ランダム」はMomoちゃん以外から無作為に
選びます。ローカルバックエンドでは画像生成を行わず、選ばれた背景を表示します。

演出は音量、波形、周波数成分に反応します。既定は「音量リング」です。演出を手動選択
するとその選択が使用されます。

通常再生中・ループ生成中のどちらでも画像エリアのクリックで再生を停止できます。
ループ生成中は同時にループ生成を解除し、手動モードへ戻ります。

## 生成パラメータ

- タイトル、ジャンル、ムード、楽器は任意指定です。
- 推論ステップ、`guidance_scale`、音楽の長さをスライダーで設定します。
- 「ボーカルなしバージョン」でインストゥルメンタルを指定できます。
- 音楽生成バックエンドはRemoteまたはLocal acestep.cppです。
- ローカルではTURBO-Q4、TURBO-Q8、XL TURBOとVRAMモードを選べます。
- ボーカル言語、BPM、キー、Seed、Thinkingも指定できます。空欄項目は自動決定されます。
- Remote選択時のみ画像サイズと向きを設定できます。

## 保存と再生

履歴行の再生ボタンを押すと、その曲のタイトルと歌詞を上部へ戻して再生します。
各曲を個別にダウンロード・削除できます。「一括ダウンロード」は、全曲のMP3と
曲名をファイル名にした歌詞TXTをZIPへまとめます。

## トラブルシューティング

- 生成が始まらない場合は、GemmaとACE-Stepのモデルパス、ローカルサーバーのログ、
  GPUメモリを確認してください。
- 12GB GPUでメモリ不足になる場合はTURBO-Q4とSTRICTを選択してください。
- Remote利用時だけ、`LLM_API_URL`、`ACE_STEP_API_URL`、`IMAGE_API_URL`への接続を
  確認してください。
- ブラウザの自動再生制限で再生されない場合は、プレーヤーの再生ボタンを押してください。

導入方法は[README](README.md#セットアップ)、環境変数は[デプロイメントガイド](DEPLOYMENT.md)
を参照してください。
